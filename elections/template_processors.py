# -*- coding: utf-8 -*-

def navigation_bar(request):
    """
    Construiremos la barra de navegación a partir de si hay un usuario autenticado o no.
    """
    # TODO check what is the selected page
    pass
    #selected = 'trabajos'
    #if request.path.split('/')[1] == 'my_offers':
    #    if len(request.path.split('/')) > 1 and request.path.split('/')[2] == 'add':
    #        selected = 'publish'
    #    else:
    #        selected = 'my_offers'
    #elif request.path.split('/')[1] == 'browsejobs':
    #    selected = ''
    #return {'authenticated': request.user.is_authenticated(),
    #        'username': request.user.username,
    #        'selected': selected,} 

